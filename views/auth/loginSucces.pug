extends ../layout
include ../mixins/card

block vars
  - var title='📓🐊 - Succes!'

block content
  if redirect
    h1.aa-title Redirecting...
    meta(http-equiv='refresh', content=`1; url=${redirect}`)
  else
    h1.aa-title Login succesvol!
