extends layout
include mixins/card

block vars
  - var title='📓🐊 - Course'

block content
  .aa-course-list-cards
    each course in courses
      +card('Ga naar course')(title=course.name, text=course.code, class=course.class)
